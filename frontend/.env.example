# Optional full websocket URL override (takes precedence)
# VITE_WS_URL=ws://localhost:8000/ws

# Used by local fallback URL logic and Vite proxy config
VITE_BACKEND_HOST=localhost
VITE_BACKEND_PORT=8000
